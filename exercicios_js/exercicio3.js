// 03) Crie uma função que recebe dois parâmetros, base e expoente, e retorne a base elevada ao expoente.

//utilie ** para calcular potenciação
function baseExpoente(a, b) {
    let base = a ** b;
    return base;
}

console.log(baseExpoente(4, 10));
