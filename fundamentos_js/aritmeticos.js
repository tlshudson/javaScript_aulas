const [a, b, c, d] = [3, 5 ,4, 1];

//operadores aritméticos sendo utilizados para atribuir valores em constantes:
const soma = a + b + c + d;
const subtracao = d - b;
const multiplicacao = a * b;
const divisao = d / a;

//o operador modulo mostra o resto da divisão, sendo 1 retornando para valores impares e 2 para valores pares;
const modulo = a % 2;

console.log(soma, subtracao, multiplicacao, divisao, modulo);